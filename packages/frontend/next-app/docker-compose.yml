version: '3.7'

services:
  next-app:
    container_name: next-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: on-failure
    volumes:
      - ./:/next-app
      - /next-app/node_modules
      - /next-app/.next
    ports:
      - 3000:3000
